filters:
  and:
    - file.hasTag("journal/diary")
formulas:
  Subject: link(file, aliases[0])
properties:
  note.when:
    displayName: When
views:
  - type: table
    name: Recent
    order:
      - when
      - formula.Subject
    sort:
      - property: when
        direction: DESC
    columnSize:
      note.when: 200
